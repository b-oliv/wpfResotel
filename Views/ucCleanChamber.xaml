<UserControl x:Class="ProjetRESOTEL.Views.ucCleanChamber"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjetRESOTEL.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>

        <TextBlock Style="{DynamicResource StyleLabel}" Grid.Row="1" Text="Liste des réservations" />


        <DataGrid Grid.Row="2" ItemsSource="{Binding Bedroom}" AutoGenerateColumns="False" CanUserResizeColumns="True" Margin="0,0,0,-394">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Numéro Chambre" Binding="{Binding IdBedroom}" />
                <DataGridTextColumn Header="Etage" Binding="{Binding FloorNumber}" />
                <DataGridTextColumn Header="Etat" Binding="{Binding IsDirty}" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
